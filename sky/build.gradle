apply plugin: 'com.android.library'

android {
    compileSdkVersion versions.compileSdk
    buildToolsVersion versions.buildTools

    defaultConfig {
        minSdkVersion versions.minSdk
    }
    lintOptions {
        textReport true
        textOutput 'stdout'
        abortOnError false
    }
}
dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    api project(':sky-core')
    api deps.support.cardview_v7

    api deps.library.nineoldandroids
    api deps.library.gson

    api deps.library.glide
    api deps.library.glide_okhttp3
    api deps.library.okhttp3
    api deps.library.java5

    annotationProcessor deps.annotation.dagger_compiler

    api group: 'commons-io', name: 'commons-io', version: "${versions.commons_io}"
    api 'com.android.support.constraint:constraint-layout:1.0.2'
}
//apply from: rootProject.file('gradle/maven-github-aar.gradle')//Github打包
apply from: rootProject.file('gradle/gradle-mvn-push.gradle')//中央库打包